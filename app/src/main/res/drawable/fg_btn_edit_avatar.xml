<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="@android:color/white" />
        </shape>
    </item>
    <item>
        <layer-list>
            <item>
                <shape android:shape="oval">
                    <solid android:color="@color/btn_edit_picture_foreground" />
                </shape>
            </item>
            <item
                android:width="@dimen/size_btn_edit_picture_icon"
                android:height="@dimen/size_btn_edit_picture_icon"
                android:drawable="@drawable/ic_insert_photo_white_24dp"
                android:gravity="center" />
        </layer-list>
    </item>
</ripple>
